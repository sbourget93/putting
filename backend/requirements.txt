# Bounded to a major version rather than exact-pinned: this is a template, and a
# fork should get security patches without a lockfile to maintain. Pin exactly
# here if a fork ever needs reproducible builds.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
boto3>=1.34,<2.0
google-auth>=2.30,<3.0
requests>=2.32,<3.0
itsdangerous>=2.2,<3.0
