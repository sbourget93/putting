# Serves the built SPA. The gateway terminates TLS and routes to us; we only
# serve static files and fall back to index.html so client-side routes resolve.
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
